define(["require", "exports"], function (Require, Exports) {
    var __extends = this && this.__extends || function (obj, parent) {
        function __() {
            this.constructor = obj
        }

        for (var n in parent) parent.hasOwnProperty(n) && (obj[n] = parent[n]);
        obj.prototype = null === parent ? Object.create(parent) : (__.prototype = parent.prototype, new __)
    };
    return __extends;
})